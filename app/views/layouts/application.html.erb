<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags always come first -->
    <%= csrf_meta_tags %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="x-ua-compatible" content="ie=edge">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <title>Avant Premiere</title>
  </head>
  <body>
    <nav class="navbar navbar-light bg-faded">
      <a class="navbar-brand" href="#">Avant Premiere</a>
      <ul class="nav navbar-nav">
        <li class="nav-item">
          <%= link_to t('.all_activities'), root_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to t('.my_activities'), activities_path(only_mine: true), class: 'nav-link' %>
        </li>
      </ul>
    </nav>

    <div id="mainContainer" class="container container-fluid">
      <%= render 'layouts/flash_messages' %>      
			<%= yield %>
    </div>

    <footer class="footer" style="background-color:#f5f5f5;height:4em;padding-top: 1em">
      <div class="container">
      <% if user_signed_in? -%>
        <%= current_user.name %> 
        <%= link_to 'log out', destroy_user_session_path, method: :delete %>
      <% end -%>
      </div>
    </footer>

  </body>
</html>
